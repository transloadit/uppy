---
"@uppy/companion": patch
---

Refresh dependencies: `@aws-sdk/*` 3.1047.0 → 3.1134.0 (now requires `^3.1134.0`, fixing an unmet `@aws-sdk/lib-storage` peer dependency on `@aws-sdk/client-s3`), `ipaddr.js` 2.5.0, `serialize-javascript` 7.1.1, `ws` 8.21.3, `moment-timezone` 0.6.4, `morgan` 1.12.1 and `p-map` 7.0.8.
