{
  "extends": ["//"],
  "tasks": {
    "build": {
      "dependsOn": ["@uppy/components#build"],
      "inputs": [
        "src/**/*.{js,ts,jsx,tsx}",
        "package.json",
        "tsconfig.json",
        "tsconfig.build.json"
      ],
      "outputs": ["lib/**"]
    },
    "build:css": {
      "dependsOn": ["@uppy/vue#build"],
      "inputs": ["../components/dist/styles.css"],
      "outputs": ["dist/styles.css"]
    }
  }
}
