{
  "extends": ["//"],
  "tasks": {
    "build": {
      "dependsOn": ["@uppy/core#build", "@uppy/dashboard#build"],
      "inputs": [
        "src/**/*.{js,ts,jsx,tsx}",
        "package.json",
        "ng-package.json",
        "tsconfig.lib.json",
        "tsconfig.lib.prod.json"
      ],
      "outputs": ["fesm2022/**", "types/**", "dist/**"]
    }
  }
}
