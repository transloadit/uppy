{
  "extends": ["//"],
  "tasks": {
    "build": {
      "dependsOn": ["^build", "@uppy/core#build"],
      "outputs": ["lib/**"]
    },
    "build:css": {
      "dependsOn": ["@uppy/components#build:css"],
      "outputs": ["dist/styles.css"]
    }
  }
}
